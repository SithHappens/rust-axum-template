[package]
name = "lib-utils"
version.workspace = true
edition.workspace = true

[lib]
doctest = false

[lints]
workspace = true

[dependencies]
base64 = "0.22"
time = { version = "0.3", features = ["formatting", "parsing", "serde"] }